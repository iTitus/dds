plugins {
    id 'java-library'
}

mavenPublishing {
    publishToMavenCentral(true)
    signAllPublications()

    pom {
        name = 'DDS Reader'
        description = 'DDS Image Library'
        url = 'https://github.com/iTitus/dds'
        licenses {
            license {
                name = 'MIT License'
                url = 'https://mit-license.org'
            }
        }
        developers {
            developer {
                id = 'iTitus'
                name = 'iTitus'
                email = 'ititus@users.noreply.github.com'
            }
        }
        scm {
            connection = 'scm:git:git://github.com/iTitus/dds.git'
            developerConnection = 'scm:git:ssh://git@github.com:iTitus/dds.git'
            url = 'https://github.com/iTitus/dds'
            tag = "v${version}"
        }
    }
}
